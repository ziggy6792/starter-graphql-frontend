import React from 'react';

import HomeScreen from 'src/screens/reel-screen';

const Routes: React.FC = () => {
  // This is where routing would be done if we had more screens
  return <HomeScreen />;
};

export default Routes;
